# Dependency directories
# These are huge and can be re-installed with 'npm install'
node_modules/

# Environment variables (CRITICAL)
# This contains your MongoDB credentials and secret keys
.env
.env.local
.env.sample

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Operating System files
.DS_Store
Thumbs.db

# Build directories (if you use TypeScript or Babel later)
dist/
build/